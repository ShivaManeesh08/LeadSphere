services:
  - type: web
    name: leadsphere
    runtime: node
    # Build: install backend deps + build React frontend
    buildCommand: npm run install-backend && npm run build
    # Start: Express serves both API and the built React app
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
